# Volumio Device IP Addresses
# Copy this file to .env and update with your actual device IPs
# The .env file is gitignored and won't be committed

VOLUMIO_DEVICE_1=192.168.1.100
VOLUMIO_DEVICE_2=192.168.1.101
VOLUMIO_DEVICE_3=192.168.1.102

# Optional: SSH username (default: volumio)
VOLUMIO_SSH_USER=volumio

# Optional: SSH password (leave empty to use SSH keys)
# VOLUMIO_SSH_PASSWORD=

# Optional: Plugin package filename
PLUGIN_PACKAGE=sendspin-plugin.zip
